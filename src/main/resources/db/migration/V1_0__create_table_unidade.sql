select * from tbl_unidade;CREATE TABLE `tbl_unidade` (
`id_unidade` integer primary key auto_increment,
`nome_unidade` varchar(100) NOT NULL,
`endereco_unidade` varchar(100) NOT NULL,
`telefone_unidade` varchar(100) NOT NULL,
)